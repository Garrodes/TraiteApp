{% extends "base.html.twig" %}

{% block title %} {{ parent() }} | InfoTraite {% endblock %}

{% block body %}

<div class="container mt-4 w-50 p-3">

  <h1> Ajouter une Information</h1>
  
  {{ form_start(form)}}
  <div class="form-group">    
          {{ form_label(form.type)}}
          {{ form_widget(form.type)}}
      <div class="form-error">
          {{form_errors(form.type)}}
      </div>          
    </div>
    <div class="form-group">
      {{ form_label(form.relatedCows)}}
      {{ form_widget(form.relatedCows)}}
      <div class="form-error">
          {{form_errors(form.relatedCows)}}
      </div>
    </div>
    <div class="form-group">
      {{form_row(form.submit)}}
    </div>
      
  {{ form_end(form)}}
  </div>
  
  </div>





{% endblock %}